# 系统&内核

## Hash Table

**哈希表（Hash Table）** ，也常被称为**散列表**，是一种通过 **“键（Key）”** 直接访问 **“值（Value）”** 的数据结构。它的核心优势是**平均 O(1) 的插入、查找和删除速度**。

为了帮你建立起从抽象概念到物理实现的完整认知，下面我将把它拆解为“**设计哲学**”、“**物理结构**”和“**核心操作流程**”三个部分来讲解。

------

### 1. 设计哲学：从“翻箱倒柜”到“直达目标”

在没有哈希表的世界里，查找一个元素如同在图书馆里找一本没有编号的书：

- **最坏情况**：你必须从第一本开始，一本一本地翻（遍历），时间复杂度是 O(n)。
- **最好情况**：书是按照字母顺序排列的（有序数组），你可以用二分查找，时间复杂度是 O(log n)。

而哈希表的思路是，**给每本书都算出一个唯一的“门牌号”，然后直接走到那个门牌号所在的位置**。这个“门牌号”就是通过**哈希函数**计算出来的。

------

### 2. 物理结构：内存里的“定制仓库”

在物理内存层面，哈希表就是一座**“带智能索引的仓库”**，由两个核心部件构成：

1. **哈希函数（Hash Function）—— 门牌号计算器**
   - 它是一个数学算法（比如你刚看过的 BKDR 算法），把任意长度的 Key（比如字符串 `"user_123"`）算成一个数字。
   - 它的目标是：**同样的 Key，永远算出一模一样的数字**。
2. **桶数组（Bucket Array）—— 带编号的货柜**
   - 这是一块**连续的内存**，像一个有 N 个抽屉的柜子，每个抽屉就是一个“桶”。
   - **寻址**：访问第 `i` 个桶，CPU 不需要遍历，直接通过 `基址 + i * 单个元素大小` 计算地址。这一步在纳秒级完成。

------

### 3. 核心操作流程：在物理内存里走一遍

当你执行 `INSERT INTO hash_table VALUES ('apple', 5);` 时，底层发生了以下物理动作：

1. **计算门牌号**：CPU 执行哈希函数，把 Key `"apple"` 算成一个整数，比如 `hash("apple")` 输出 `1000`。
2. **取模定位**：仓库只有 8 个抽屉，通过取模运算定位抽屉：`1000 % 8 = 0`。所以这条数据被**存放在 0 号桶**。
3. **物理写入**：CPU 通过 `基址 + 0*8字节` 计算出 `0号桶` 的地址，然后把数据写入。

当你查找 `SELECT * FROM hash_table WHERE key = 'apple';` 时，同样的流程走一遍，CPU 直接去 `0号桶` 取数据，**不需要扫描整个仓库**。

------

### 4. 哈希冲突与解决（你在源码里必见）

如果 `hash("apple") % 8 == 0`，而 `hash("banana") % 8` 也等于 0，这就是**哈希冲突**。

**SeekDB 的解决方法（拉链法）**：

- **物理表现**：仓库的 `0号抽屉` 里不再只放一个苹果，而是放一个**“小篮子”（链表）**。
- **插入时**：把 `"apple"` 和 `"banana"` 都挂在这个小篮子里。
- **查找时**：先去 `0号抽屉`，再在小篮子里逐个比对，找到 `"apple"`。

------

### 5. SeekDB 源码里的哈希表（实战定位）

结合你之前看过的**容器文档**和源码结构，SeekDB 的哈希表实现遵循“性能为王”的原则：

- **主实现**：`ObHashMap` 基于 **拉链法**，允许你**自定义内存分配器和锁策略**，完美适配数据库的大批量、高并发场景。
- **无锁版本**：`ObLinkHashMap` 基于**无锁算法**实现，读写性能和线程安全均十分出色。

------

### 💎 总结：一句话穿透本质

**哈希表就是用“数学计算（哈希函数）”去换取“搜索速度（O(1)）”的一种数据结构。** 它把一个无序的、需要遍历查找的问题，转化成了“先算数、再直达”的物理寻址问题。

当你理解了这一点，再看 `src/sql/engine/aggregate/ob_hash_aggregate_op.cpp` 里的代码，你就会明白：**`GROUP BY` 之所以快，不是因为数据库有魔法，而是因为它内部构建了一个哈希表，把每一行的 `user_id` 都当成门牌号，瞬间就把数据归类好了。**



## Mem Table

**MemTable** 是 LSM-Tree 存储引擎的内存核心，负责缓冲所有最新的写入操作。

它的本质是一个**内存数据结构**，在 SeekDB 这样基于 LSM-Tree 的数据库中，作为写入操作的第一站和查询操作的最新数据源。

### 核心使命：写入缓冲与数据排序

MemTable 的设计遵循 LSM-Tree **"将随机写入转化为顺序写入"**的核心哲学，其职责可以概括为两点：

1. **高效吸收写入**：所有 `INSERT`、`UPDATE`、`DELETE` 操作都会先写入 MemTable。由于是纯内存操作，这个过程非常快，能提供**亚毫秒级的写入延迟**。
2. **保持数据有序**：MemTable 内部使用有序数据结构（如跳表），将写入的 Key 实时排序。这为后续将数据**顺序写入**磁盘（转为 SSTable）做好了准备，极大地提升了磁盘 I/O 效率。

### 内部构造与操作

- **底层数据结构**：为了实现高性能的有序存储，MemTable 通常使用**跳表 (SkipList)** 实现。跳表在读写性能上与平衡树（如红黑树）相当，但实现更简单。
  - **SeekDB (OceanBase) 的特殊实现**：为了平衡**单点查询**和**范围查询**的性能，其 MemTable 由 **B-Tree 和 HashTable** 组合而成。其中 HashTable 用于加速单行数据的快速定位，而 B-Tree 则负责维护数据的有序性，以支持高效的范围扫描。
- **关键操作**：
  - **写入 (Put)**：将一条 Key-Value 数据插入 MemTable。在 OceanBase 的实现中，`UPDATE` 和 `DELETE` 也会作为一条**带有特殊标记和时间戳的新记录**写入。
  - **查询 (Get)**：根据 Key 在 MemTable 中查找对应的 Value。
  - **内存估算 (appro_memory_usage)**：持续监控自身占用的内存大小，以判断是否达到触发“转储”的阈值。

### 生命周期：从活跃到冻结

MemTable 的生命周期清晰地体现了 LSM-Tree 的“内存-磁盘”两级存储结构。

1. **活跃 (Active)**：当前的**活跃 MemTable**，接收所有新的写入操作。
2. **冻结 (Immutable)**：当活跃 MemTable 的大小达到预设阈值（如 64MB）时，它会被标记为**只读的 Immutable MemTable**。此时，系统会创建一个新的活跃 MemTable 来接收后续写入，从而保证写入不中断。
3. **转储 (Flush)**：后台进程会将 Immutable MemTable 中的数据**顺序写入**磁盘，形成一个或多个 **SSTable 文件**。
4. **销毁**：数据成功转储到 SSTable 后，Immutable MemTable 占用的内存会被释放。

### 数据读取

数据读取时，会**首先在 MemTable 中查找**。因为 MemTable 包含最新数据，必须被优先查询。只有 MemTable 中没有找到，才会去查询磁盘上的 SSTable。

### 总结

MemTable 是 LSM-Tree **高性能写入**的保证。它将随机写入转化为内存中的有序操作，为后续高效的**顺序磁盘写入**奠定了基础。同时，它作为**最新数据的驻留地**，确保了查询能第一时间获取到最新结果。



## SSTable

**SSTable 是 Sorted String Table（排序字符串表）的缩写**。你可以把它理解为 LSM-Tree 存储引擎在磁盘上的“最终交付物”。

在 SeekDB 这类基于 LSM-Tree 的数据库中，SSTable 是**存储在磁盘上的、只读的、按 Key 有序排列的键值对（Key-Value）文件**。

### 💡 核心特征

- **有序性**：SSTable 内部的所有 Key 都是**严格排序**的。这保证了在磁盘上也能使用二分查找等高效算法快速定位数据。
- **不可变性**：SSTable **一旦生成就绝不会再被修改**。这简化了并发控制（无需加锁），并让磁盘写入变为高效的**顺序写**，而非随机写。
- **持久性**：SSTable 是数据在磁盘上的最终存储形式，确保了数据的持久化。

### 🏛️ 在 LSM-Tree 中的角色：从内存到磁盘

SSTable 是 LSM-Tree 写入路径的终点和读取路径的起点。

- **写入路径**：数据先写入内存中的 **MemTable**。当 MemTable 写满后，它会被**冻结**并直接**顺序写入**磁盘，形成一个 **SSTable 文件**。这个将内存数据刷到磁盘的过程，就是之前提到的 **Mini Compaction（转储）** 。
- **读取路径**：查询时，会先在最快的 **MemTable** 中查找，如果没找到，再依次查找磁盘上各个层级的 **SSTable**。

### 🗂️ SeekDB (OceanBase) 中的 SSTable：三层结构

SeekDB 将 SSTable 分为三个层级进行管理。这正是 LSM-Tree 中 **Tiered & Leveled Compaction 策略**的具体体现。

- **L0 层：Mini SSTable**
  - **来源**：由 MemTable 直接转储（Flush）产生。
  - **特点**：数据较新，可能包含多个文件且 Key 范围**有重叠**。
  - **作用**：作为快速吸收写入的“缓冲层”。
- **L1 层：Minor SSTable**
  - **来源**：由多个 L0 层的 Mini SSTable 合并（Minor Compaction）产生。
  - **特点**：文件数较少，Key 范围通常**不重叠**，结构更紧凑。
  - **作用**：整理数据，提升查询效率。
- **L2 层：Major SSTable**
  - **来源**：由 L1 层与 L2 层数据合并（Major Compaction / 全局合并）产生。
  - **特点**：包含全部基线数据，是**最底层、最完整**的数据快照，通常全局唯一。
  - **作用**：作为查询的最终数据来源和空间回收的基石。

### 🔬 微观结构：SSTable 的内部组成

一个 SSTable 文件内部也有精心设计的结构。

- **宏块 (Macro Block)**：SSTable 由若干个 **2MB** 的定长宏块组成。宏块是磁盘**写入 IO** 的基本单位。
- **微块 (Micro Block)**：宏块内部又划分为多个 **16KB** 左右的变长微块。微块是磁盘**读取 IO** 的最小单位，且通常会被**压缩**存储以节省空间。
- **元数据 (Metadata)**：SSTable 文件头部还包含自身的元数据信息，用于快速定位和管理内部的数据块。

> 为了让你对这个“物理分层”有更直观的感受，可以参考下面的图示：
>
> text
>
> ```
> [ 内存 ]    MemTable (读写)
>    ↓ 转储 (Mini Compaction)
> [ L0 层 ]   Mini SSTable (多个, Key 有重叠)  ← Tiered 模式
>    ↓ 合并 (Minor Compaction)
> [ L1 层 ]   Minor SSTable (少数, Key 不重叠) ← Leveled 模式
>    ↓ 合并 (Major Compaction)
> [ L2 层 ]   Major SSTable (单个, 基线数据)   ← Leveled 模式
> ```

### 💎 总结

SSTable 是 LSM-Tree 在磁盘上的物理体现。它通过**有序、只读、分层**的设计，将内存中的随机写入转化为磁盘的顺序写入，从而实现了极高的写入吞吐量。



## mmap(Memoy Map 内存映射)

将磁盘文件或设备（或匿名物理内存）地址空间，映射到进程的虚拟内存地址空间中，使得应用程序可以通过指针直接访问这些资源，而无需显示的I/O系统调用。

| 组合          | 含义                           | 物理动作                                                   | 典型应用                                     |
| ------------- | ------------------------------ | ---------------------------------------------------------- | -------------------------------------------- |
| 文件映射+共享 | 多进程共享同一份文件缓存       | 修改内存立即回写Page Cache，其他进程可见，最终同步到磁盘。 | 数据库共享缓冲池（多个进程共享同一份数据页） |
| 文件映射+私有 | 多进程共享原文件，但写入时隔离 | COW(写时复制)：读时共享原页，写时复制私有一份新物理页。    | 加载共享库（.so）                            |
| 匿名映射+共享 | 不关联文件，多进程共享内存     | 物理内存由操作系统从Swap或物理内存池分配，多个进程可见     | 进程间通信。                                 |
| 匿名映射+私有 | 不关联文件，进程独享           | 直接向操作系统申请空白物理内存就，不关联任何文件           | malloc申请大块内存的底层实现(>128KB)。       |

匿名映射：一块没有“文件名”的物理内存，由操作系统用“零”初始化，专供进程内部或进程间使用，用完即弃，绝不回写任何磁盘文件。

在seekdb里面的共享缓冲池（内存管理层面）是匿名映射+私有

### 为什么不选 ② 私有 + 文件（mmap 数据文件）

- 生命周期不一致：AChunkMgr 需要「按应用语义」申请/释放/淘汰（LRU、洗脏、压缩），文件 mmap 的页生命周期跟 fd 绑死，无法做精细管理
- 双重 page cache：OS 已经把文件页缓在 page cache 里；如果再 mmap 应用层也维护一套缓存 → 内存浪费 + 写放大
- 绕过应用 I/O 栈：磁盘数据要经过「解 SST 宏块头 → 解 micro block 编码 → 反查 bloom filter」才能用，文件 mmap 拿到的是裸字节，没法直接吐给查询

### 为什么不选 ③ 共享 + 匿名

- 进程模型不匹配：OceanBase/seekdb 是 单进程多线程 架构，线程天然共享进程地址空间，不需要 MAP_SHARED
- 共享 + 匿名用法一般在父子进程通信、跨进程 KV 里出现，seekdb 没这种需求

### 为什么不选 ④ 共享 + 文件

- 同 ③，单进程不需要 MAP_SHARED
- 同 ②，文件 mmap 与应用 page cache 的管理语义冲突
- 数据库持久化已经有了自己的「WAL + SST + 副本 + Paxos」栈，不需要把内存挂到文件上

### 为什么 ① 是最优解

1. 进程模型匹配 单进程多线程 → 线程通过地址空间天然共享 chunk，不需要跨进程共享机制。
2. 让 OS 当调度器，应用掌控语义 匿名 mmap 给的是「虚拟地址空间区域 + 物理页懒分配」，AChunkMgr 在这上面自己建 size-class freelist：

```cpp
// achunk_mgr.cpp
void *AChunkMgr::low_alloc(const uint64_t size) {
  const int flags = MAP_PRIVATE | MAP_ANONYMOUS;
  ptr = ::mmap(nullptr, size, prot, flags, fd, offset);  // OS 只负责分配虚拟页
}
```

3. 与 `MADV_DONTNEED` 配合做"软释放"

```cpp
int result = this->madvise((char*)chunk + hold_size, orig_hold_size - hold_size, MADV_DONTNEED);
```

私有匿名页 → `MADV_DONTNEED` 让内核立刻回收物理页但保留 VA 映射 → 既能 shrink 内存又避免 munmap/mmap 抖动。文件 mmap 和共享映射都没这么好的「软释放」语义。

4. 避免 fork 后的 COW 风暴 私有 + 匿名在 `fork` 时不会复制任何已写入页（没东西可写）。shared 映射会在 fork 后变成父子共享，污染语义。
5. 一致性可控 应用层自己处理 dirty/flush/evict，不用经过 `msync`、`mprotect` 这些语义多变、跨内核行为不一致的系统调用。