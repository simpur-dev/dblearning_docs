# seekdb

## 文件即 SQL 表

### 赛题名称

2026 OceanBase 数据库大赛：**File as SQL 功能实现**（公开接口风格主要参考 DuckDB）

### 赛题核心目标

实现 “文件即 SQL 表” 的即席查询能力：用户无需执行`CREATE TABLE`建表、无需执行`LOAD DATA`导入数据，直接通过 SQL 语句即可查询本地磁盘上的结构化数据文件，同时支持查询结果导出，完整覆盖「发现文件→识别结构→查询分析→结果导出」的全流程。

------

### 详细题目要求

#### 一、功能能力要求

##### 1. 基础 SQL 接口（零 DDL 即席查询）

- 提供文件级表函数：`read_csv()`、`read_parquet()`、`read_json()`（对应 JSONL 格式），直接在`FROM`子句中传入文件路径即可作为表查询。
- 提供目录扫描能力：通过`glob()`函数查询指定目录下的文件列表，支持通配符匹配。
- 提供 Schema 探查能力：支持`DESCRIBE SELECT * FROM read_xxx('文件路径')`语法，自动推断并输出文件的字段名、字段类型等结构信息。
- 支持结果导出：提供`COPY (查询SQL) TO '目标文件路径' (FORMAT 格式, 配置项)`语法，将查询结果导出为本地文件。

##### 2. 支持的文件格式

- **CSV 格式**：支持自定义分隔符、引号、转义符、表头开关、全字符类型、空字符串处理等特性。
- **JSONL（行式 JSON）格式**：支持 union Schema、缺失字段兼容、类型宽化、嵌套 JSON 拒绝等能力。
- **Parquet 格式**：支持 V2 版本、Snappy/PLAIN 压缩、Dictionary 编码、INT96 类型等主流特性。

##### 3. SQL 计算能力

- 单文件查询：支持 SELECT 投影、表达式计算、列别名；支持 WHERE 过滤、ORDER BY 排序、LIMIT 截断；支持 GROUP BY 分组聚合、HAVING 条件，兼容 COUNT、SUM、AVG、MIN、MAX 等聚合函数。
- 多文件关联：支持两个本地文件的 JOIN 查询，覆盖 INNER JOIN、LEFT JOIN，支持不同格式文件之间的关联（如 CSV 与 Parquet 关联）。

##### 4. 确定性约束

- 类型推断可复现：对同一份文件的字段类型推断结果固定，支持自动推断`BIGINT、DOUBLE、BOOLEAN、VARCHAR、DATE、DATETIME、NULL`等数据类型。
- 查询行为可复现、错误表现可复现，整体能力适配自动化测试场景。

#### 二、架构与执行边界要求

1. **核心计算必须走原生 SQL 引擎**：JOIN、聚合、排序等核心 SQL 算子必须由数据库自身的 SQL 执行引擎完成，禁止在文件读取层旁路执行计算。
2. **模块分层与分工**
   - 文件读取层（File Reader）：仅负责文件 I/O 读取、格式解析、类型转换，最终输出标准化的批量数据（ObDatum batch）给上层 SQL 引擎。
   - SQL 引擎层：负责表达式计算、数据过滤、多表关联、聚合计算、排序等全量 SQL 逻辑执行。
3. **代码架构流程**：SQL 输入 → 解析器（Resolver，包含路径安全校验 Path Guard + 自动 Schema 推断） → 查询优化器 → 文件扫描算子（File Scan） → 统一格式数据输出 → SQL 执行引擎计算。

#### 三、安全边界要求

- 文件访问默认只读，禁止修改源数据文件。
- 实现目录白名单机制，仅允许访问授权目录内的文件。
- 具备路径校验能力，禁止路径穿越、越界访问。
- 提供类似`secure_file_priv`的权限管控能力。

#### 四、工程与测试要求

1. 语法兼容性：兼容 DuckDB 风格的 File SQL 语法，支持表函数别名、named options 配置项等语法特性。
2. 分层测试要求
   - Parser 单测：覆盖新增 File SQL 语法解析、旧语法兼容、表函数别名、JOIN 语法、glob、DESCRIBE、EXPLAIN、COPY 语法等场景。
   - Reader/Schema 单测：覆盖三类文件格式的类型推断、格式特性、流式读取、异常文件处理、投影裁剪、目录指纹等场景。
   - 端到端测试：覆盖功能全链路，包含开关默认状态、失败路径、各格式查询、聚合、关联、导出回读等场景。
3. 质量基线：满足可用性、安全性、性能、可维护性、可演进性的基础质量标准。